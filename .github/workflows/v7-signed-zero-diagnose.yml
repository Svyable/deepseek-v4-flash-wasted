name: Validate V7 FP8 signed-zero oracle fix (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Signed-zero oracle fix validated against inherited real attention gates."
