name: Promote bit-exact V7 F32 HC oracle (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Completed at V7 F32 HC oracle binding; manual audit marker only."
