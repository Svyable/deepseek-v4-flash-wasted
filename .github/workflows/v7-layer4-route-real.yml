name: Acquire real chained layer-4 V7 route boundary (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked after oracle-boundary failures. Recreate only after cheap F32 oracle validation is green."
