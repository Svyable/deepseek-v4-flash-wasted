name: Acquire exact chained layer-4 V7 route boundary (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked. Exact layer-4 route was acquired, uploaded, and replayed green in run 31438142112."
          echo "Do not reacquire; use the frozen fixture and permanent no-network replay."
