name: Acquire real chained layer-4 V7 route boundary (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked after a signed-zero KV differential. Re-enable only after cheap FP8/QAT diagnostics are green."
