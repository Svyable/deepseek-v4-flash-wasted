name: Acquire exact chained layer-4 V7 route boundary (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked after review found an over-strong correction-bias visibility assertion."
          echo "Re-enable only after the no-network router contract is corrected and green."
