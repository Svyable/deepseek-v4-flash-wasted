name: Acquire exact chained layer-4 V7 route boundary (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked after checkpoint arithmetic and route discovery passed."
          echo "Compact replay needs its tools import fixed before reacquisition."
