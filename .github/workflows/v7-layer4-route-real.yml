name: Acquire real chained layer-4 V7 route boundary (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked after one-row online-softmax signed-zero differential."
          echo "Re-enable only after the permanent no-network attention boundary regression is green."
