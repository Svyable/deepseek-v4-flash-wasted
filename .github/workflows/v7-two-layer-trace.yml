name: Build and validate real V7 two-layer trace (parked)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Parked: bit-exact F32 HC review found the frozen Gate-H layer-3 endpoint differs from current C semantics."
          echo "Correct Gate H and propagate the new layer-3 state before rebuilding the V7 trace."
